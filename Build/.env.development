# Dev-only overrides, loaded by `npm run dev` (mode: development).
# Points the app at a locally-running Worker (`wrangler dev`, default port 8787).
# Production builds ignore this file.
PUBLIC_WORKER_URL=http://localhost:8787
