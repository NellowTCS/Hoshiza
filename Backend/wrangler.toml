name = "hoshiza"
main = "src/worker.ts"
compatibility_date = "2026-01-01"

# Runtime vars. The client id is public (from the GitHub OAuth app settings page);
# the client secret is a deploy secret set with:
#
#   npx wrangler secret put GITHUB_CLIENT_SECRET

[vars]
GITHUB_CLIENT_ID = "Ov23lihmmQk8tFShpHHq"
APP_URL = "https://nellowtcs.me/Hoshiza"
